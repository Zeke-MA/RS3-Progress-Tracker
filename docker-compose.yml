services:
  postgres:
    image: postgres:latest
    container_name: rs-postgres
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
    driver: local
  redis_data:
    driver: local
